apiVersion: batch/v1
kind: Job
metadata:
  name: user-service-migrations
  namespace: fantasy-maps
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migrations
          image: fantasy-maps-user-service:local
          imagePullPolicy: IfNotPresent
          command: ["alembic", "upgrade", "head"]
          envFrom:
            - secretRef:
                name: user-service-secret